# Automatically created by: gerapy
from setuptools import setup, find_packages
setup(
    name='HCK_l',
    version='1.0',
    packages=find_packages(),
    entry_points={'scrapy':['settings=HCK_l.settings']},
)